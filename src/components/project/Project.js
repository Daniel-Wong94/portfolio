import { Section } from "../section";
import styles from "./Project.module.css";

const Project = () => {
  return (
    <Section paddingY="m" bgColor="Tertiary2">
      <div>Projects</div>
    </Section>
  );
};

export default Project;
